@model Picklist

@{
    ViewBag.Title = "Edit Picklist";
}

<h2>Update @Model.QuestionTitle</h2>

@using (Html.BeginForm(null, null, FormMethod.Post, new { data_ajax="false" }))
{
    @*<div class="ui-grid-c">

    @{int columnNumber = 1;}

    @for (int i = 0; i < Model.Mappings.Count; i++)
    {
        KeyValuePair<string, string> keyValuePair = Model.Mappings.ToList()[i];
        
        <div class="ui-block-@Html.ColumnLetter(columnNumber)">   
            <button name="pickListButton-@keyValuePair.Key" type="submit" value="@keyValuePair.Key" data-mini="false">@Html.Translate(keyValuePair.Value)</button>
        </div>
        
        columnNumber = columnNumber == 4 ? 1 : columnNumber + 1;


    }
    </div>*@
    
    @Html.HiddenFor(model => model.QuestionId)

    @Html.DropDownListFor(m => m.MappingKey, Model.SelectListItems, new { data_theme = "b" })

    <div class="container_12">
        <div class="grid_6">
            @Html.ActionLink("Cancel", "Index", null, new { data_role = "button", data_icon = "delete", data_iconpos = "left", data_inline = "true", data_mini = "false" })             
        </div>	   
        <div class="grid_6 right">
            <input type="submit" data-icon="check" data-iconpos="right" data-inline="true" data-mini="false" value="Update" />
        </div>	   
    </div>
}